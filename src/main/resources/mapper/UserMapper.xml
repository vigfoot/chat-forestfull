<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.forestfull.domain.UserMapper">
    <!-- 회원 조회 by username -->
    <select id="findByUsername" resultType="com.forestfull.domain.User" parameterType="String">
        SELECT
            m.id
            , m.name
            , m.password
            , m.roles
        FROM chat_forestfull.member m
        WHERE m.name = #{name}
    </select>

    <select id="findByUserId" resultType="com.forestfull.domain.User" parameterType="long">
        SELECT
            m.id
            , m.name
            , m.roles
            , m.display_name
            , m.profile_image
            , m.created_at
            , m.created_by
            , m.updated_at
            , m.updated_by
        FROM chat_forestfull.member m
        WHERE m.id = #{userId}
    </select>

    <select id="findUserIdById" resultType="long" parameterType="long">
        SELECT id FROM chat_forestfull.member WHERE member.id = #{id} LIMIT 1
    </select>

    <!-- 회원 생성 -->
    <insert id="save" parameterType="com.forestfull.domain.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO chat_forestfull.member (name, password, roles, display_name, profile_image, created_by, updated_by)
        VALUES (#{name}, #{password}, #{roles}, #{displayName}, #{profileImage}, #{createdBy}, #{updatedBy})
    </insert>

    <!-- 모든 회원 조회 -->
    <select id="findAllUsers" resultType="com.forestfull.domain.User">
        SELECT m.id, name, roles, display_name, profile_image, created_at, updated_at, created_by, updated_by
        FROM chat_forestfull.member m
    </select>

    <!-- 회원 권한만 조회 -->
    <select id="findRolesByUsername" resultType="String" parameterType="String">
        SELECT roles
        FROM chat_forestfull.member
        WHERE name = #{name}
    </select>

    <!-- 회원 비밀번호 업데이트 -->
    <update id="updatePassword" parameterType="com.forestfull.domain.User">
        UPDATE chat_forestfull.member
        SET password = #{password},
            updated_at = CURRENT_TIMESTAMP,
            updated_by = #{updatedBy}
        WHERE id = #{id}
    </update>

    <!-- 회원 비밀번호 업데이트 -->
    <update id="updateRoles">
        UPDATE chat_forestfull.member
        SET roles = #{roles}
        WHERE id = #{id}
    </update>

    <!-- 회원 삭제 -->
    <delete id="deleteById" parameterType="long">
        DELETE FROM chat_forestfull.member
        WHERE id = #{id}
    </delete>

    <select id="getRolesByUserId" parameterType="long" resultType="long">
        SELECT roles FROM chat_forestfull.member WHERE id = #{id}
    </select>
</mapper>