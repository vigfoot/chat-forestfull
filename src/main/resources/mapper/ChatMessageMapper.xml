<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.forestfull.chat.message.ChatMessageMapper">

    <!-- 메시지 저장 -->
    <insert id="saveMessage" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO chat_forestfull.chat_message (room_id, member_id, message, created_by)
        VALUES (#{roomId}, #{memberId}, #{message}, #{sender})
    </insert>

    <!-- 최근 메시지 조회 -->
    <select id="findRecentMessages" resultType="com.forestfull.chat.ChatDTO$Message">
        SELECT id, room_id, member_id, message, sent_at, created_by AS sender
        FROM chat_forestfull.chat_message
        WHERE room_id = #{roomId}
        ORDER BY sent_at DESC
        LIMIT #{limit}
    </select>
</mapper>