<?xml version="1.0" encoding="UTF-8"?>
<configuration scan="true">

    <!-- 콘솔 Appender: 파란색 SQL 출력 -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <!-- %highlight 로 ERROR/INFO 등 기본 색 유지 + SQL 로그는 파란색 -->
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %highlight(%logger) - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- MyBatis SQL 로그를 파란색으로 강제 하이라이트 -->
    <logger name="org.mybatis" level="DEBUG">
        <appender-ref ref="CONSOLE"/>
    </logger>
    <logger name="java.sql.PreparedStatement" level="DEBUG">
        <appender-ref ref="CONSOLE"/>
    </logger>
    <logger name="java.sql.Connection" level="INFO"/>
    <logger name="java.sql.Statement" level="DEBUG">
        <appender-ref ref="CONSOLE"/>
    </logger>
    <logger name="java.sql.ResultSet" level="WARN"/>

    <!-- File Appender: PROD 에서만 동작 -->
    <springProfile name="prod">
        <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
            <file>logs/app.log</file>
            <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
                <fileNamePattern>logs/app.%d{yyyy-MM-dd}.gz</fileNamePattern>
                <maxHistory>14</maxHistory>
            </rollingPolicy>
            <encoder>
                <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger - %msg%n</pattern>
            </encoder>
        </appender>

        <!-- prod root logger: console + file -->
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
            <appender-ref ref="FILE"/>
        </root>
    </springProfile>

    <!-- DEFAULT root logger: console only -->
    <springProfile name="!prod">
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
        </root>
    </springProfile>

</configuration>